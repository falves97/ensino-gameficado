{% extends 'base.html.twig' %}

{% block title %}Redefina sua senha{% endblock %}

{% block header %}{% endblock %}
{% block body %}
    <div class="bg-gradient-to-r from-blue-300 to-blue-500 min-h-screen flex items-center justify-center">
        <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md relative">
            <div class="absolute -top-20 left-1/2 transform -translate-x-1/2">
                <img src="{{ asset('images/logo.jpg') }}" alt="Mascot"
                     class="w-32 h-32 object-cover rounded-full border-4 border-white shadow-lg">
            </div>

            {% form_theme requestForm with ['tailwind_2_layout.html.twig', 'form/fba_theme.html.twig'] %}

            {% for flash_error in app.flashes('reset_password_error') %}
                <div class="p-4 mb-4 text-sm text-red-800 rounded-lg bg-red-50" role="alert">{{ flash_error }}</div>
            {% endfor %}

            <div class="pt-12">
                <h1 class="text-3xl mb-6 text-blue-600 text-center">Esqueceu a Senha?</h1>

                {{ form_start(requestForm, {
                    attr: {
                        class : 'space-y-4'
                    }
                }) }}
                {{ form_row(requestForm.email) }}

                <button type="submit"
                        class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition duration-150 ease-in-out">
                    Enviar e-mail de redefinição
                </button>
                {{ form_end(requestForm) }}

                <div class="mt-6 text-center">
                    <p class="text-sm text-gray-600">Lembrou a sua senha?
                        <a href="{{ path('app_login') }}"
                           class="font-medium text-blue-600 hover:text-blue-500"
                        >Voltar para o login</a></p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block footer %}{% endblock %}
